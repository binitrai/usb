import React from 'react';
import Page from '../Page';
import './index.scss';

export default () => {
    return (
        <Page header={false}>
            <div className='login-container'>Login Page</div>
        </Page>
    )
}